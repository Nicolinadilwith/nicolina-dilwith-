# Pricing power

Capability placeholder — first engagement not started yet.

Once a real engagement lands here, this folder holds the work that proves the capability: the `spec.md` defining it, and the model/analysis files that back it up. See `docs/briefs/` for the scoping that comes before this work and `docs/decisions/` for the recommendation that comes after.
